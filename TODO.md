# TODO

- [x] Read existing search + navigation code (search.html, script.js, search-data.js).
- [x] Add query-based exact-match selection using `highlight` param.
- [x] Update `buildCardUrl()` to include `highlight`.
- [x] Improve card highlighting selection when multiple matches exist.
- [x] Make highlight glow animation smoother and more noticeable (update CSS).
- [ ] Smoke test in browser: search -> click result -> smooth scroll + highlight persists ~7s.
- [ ] Verify UI never shows internal HTML filenames in search UI text.
- [ ] Verify responsiveness on mobile/tablet/desktop.

